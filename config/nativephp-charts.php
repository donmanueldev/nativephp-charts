<?php

return [
    /*
    | Application presets are merged over the matching built-in preset. A custom
    | name starts from `default`, so applications only need to publish overrides.
    */
    'presets' => [],
];
